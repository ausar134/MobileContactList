<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ContactBook.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding NavigateToContactDetailsCommand}" Text="Add new contact"/>
    </ContentPage.ToolbarItems>
    <StackLayout
        Padding="20,20,20,20"
        HorizontalOptions="CenterAndExpand"
        VerticalOptions="FillAndExpand">
        <ListView ItemsSource="{Binding People}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="{Binding FirstName}"/>
                            <Label Grid.Column="1" Text="{Binding LastName}"/>
                            <Label Grid.Column="2" Text="{Binding MobileNumber}"/>
                            <Label Grid.Column="3" Text="{Binding Email}"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Header>
                <StackLayout Orientation="Horizontal">
                    <!--<Label Text="First Name"/>
                    <Label Text="Last Name"/>
                    <Label Text="Internal Number"/>
                    <Label Text="Mobile Number"/>
                    <Label Text="Email"/>-->
                </StackLayout>
            </ListView.Header>
        </ListView>
    </StackLayout>

</ContentPage>
